export type NavigationTypes = {
  navigation: {
    darkMode: boolean;
    searching: boolean;
    mobileMenu: boolean;
  };
};

export type EventsTypes = {
  events: {
    eventsCardsDisplay: boolean;
  };
};
